# 任务分级规则

## 1. 分级原则

等级用于控制文档成本。它基于风险和协作复杂度，不单纯依据代码行数或排期天数。

## 2. 判断表

| 维度 | L1 小改动 | L2 标准功能 | L3 复杂或高风险项目 |
| --- | --- | --- | --- |
| 用户影响 | 局部修复，行为明确 | 新增或调整完整用户行为 | 多角色、多流程或重要业务闭环 |
| 技术范围 | 单点、低耦合 | 一个或少数模块 | 跨系统、跨团队或多个核心模块 |
| 接口与数据 | 不改公共契约 | 局部可兼容变化 | 新公共接口、持久化、迁移或协议变化 |
| 风险 | 易验证、易回滚 | 中等风险 | 安全、资金、隐私、数据损坏、难回滚 |
| 决策 | 基本无方案分歧 | 有少量设计选择 | 有重大选型、替代方案或长期影响 |
| 测试 | 定向检查或人工验证即可 | 需要单元/集成与受影响路径冒烟 | 按风险增加白盒、契约、E2E、故障、性能或发布验证 |

只要命中一个明显的 L3 条件，就按 L3 管理相关部分；不要求把所有 L3 章节写满。

## 3. 最低产物

### L1 小改动

- 一份 `TASK`，可以直接放在 Issue、任务卡或 PR/MR 描述中。
- 完成验收和交付检查；Align 只确认 TASK、实现和验证一致。
- 不要求单独 PRD、SDD 或测试方案。

### L2 标准功能

- 一份 `SPEC.md`，集中记录需求、设计、测试和交付信息。
- 关键决定可直接写在 SPEC；只有长期或重大决定才另建 ADR。
- Align 逐项检查 SPEC 的 AC、关键设计和验证结果，结论放在交付清单。
- 不要求把 SPEC 拆成多份文档。

### L3 复杂或高风险项目

- 从 [正式功能模板包](templates/FORMAL-FEATURE/README.md) 开始，使用完整正规流程。
- `PRD.md`：需求意图、质量要求、假设与验收基线。
- `CLIENT-SDD.md`、`SERVER-SDD.md` 或单份 `SDD.md`：按受影响端记录关键设计、责任边界、失败处理和兼容回滚。
- `TEST-PLAN.md`：阻断级映射、跨层验证、专项测试和测试准入/退出条件。
- `DELIVERY.md`：交付基线、最终实现、测试结果、偏移、Align 和交付决定的唯一汇总。
- `ADR`：仅用于重大且难逆的决策。
- Align 对齐 PRD、SDD、ADR、可执行契约和测试证据；默认记录在 DELIVERY，仅独立审计场景另建 ALIGNMENT-GATE。
- PRD 和 DELIVERY 保持单一；Client/Server 设计都具有独立复杂度时拆分 SDD，只影响一端时只创建该端文件。

## 4. 验证与 Review 强度

文档等级控制最低产物，执行时再按实际风险选择验证档位；两者通常对应但不强制绑定。

| 档位 | 通常适用 | 最低验证 | Review |
| --- | --- | --- | --- |
| FAST | L1、低风险且易回滚 | 定向构建/测试或可复现人工验证；影响启动或用户主路径时加冒烟 | 作者自检；遵循团队原有 PR 要求 |
| STANDARD | 大多数 L2 | 单元 + 必要集成；Client/Server 受影响路径冒烟；跨端变化做契约/联调验证 | 作者自检 + 独立同事或只读 AI Review |
| CRITICAL | L3 或高风险变化 | STANDARD + 命中风险的白盒、E2E、故障注入、迁移/回滚、安全、性能验证 | 独立 Review；高风险结论由 Owner 接受 |

所有档位都必须验证本次改变的行为。CRITICAL 不等于执行所有测试类型，只执行能证明已识别风险的检查。

以下任一项通常触发 CRITICAL：公共契约、持久化或迁移、并发和状态机、安全/资金/隐私、跨服务一致性、难回滚、性能热点或重复事故。

## 5. 快速判断

```text
是否涉及重大业务风险、公共契约、数据迁移、跨系统或难回滚？
├─ 是 → L3
└─ 否
   └─ 是否形成新的完整行为，或需要解释设计与多层验证？
      ├─ 是 → L2
      └─ 否 → L1
```

## 6. 升降级

- 实现中出现新风险可以随时升级。
- Intake 影响扫描发现公共契约、持久化、跨系统、安全、迁移或难回滚风险时，必须重新判断等级，不能沿用原来的轻量结论。
- 需求收缩后可以降级，但已经产生且仍有维护价值的设计决定应保留。
- 不允许为了规避评审故意拆成多个 L1。
- 不允许仅因模板已经创建就维持不必要的高等级。
