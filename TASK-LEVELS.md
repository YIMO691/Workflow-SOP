# 任务分级规则

## 1. 分级原则

L1/L2/L3 控制文档成本，依据需求、设计与协作是否需要分别维护；FAST/STANDARD/CRITICAL 控制验证强度，依据实际影响、兼容性和失败后果。先分别判断，再说明理由；不按代码行数、排期或技术关键词自动升级。

## 2. 判断表

| 维度 | L1 小改动 | L2 标准功能 | L3 需分开维护的复杂项目 |
| --- | --- | --- | --- |
| 用户影响 | 局部修复，行为明确 | 新增或调整完整用户行为 | 多角色、多流程或重要业务闭环 |
| 技术范围 | 单点、低耦合 | 一个或少数模块 | 跨系统、跨团队或多个核心模块 |
| 接口与数据 | 无需另行解释设计 | 局部兼容变化，设计可在 SPEC 说清 | 契约、迁移与发布需跨方协调，单份 SPEC 难以清楚维护 |
| 风险记录 | 简短任务说明足够 | SPEC 可说清风险、控制与回滚 | 风险控制涉及多个责任方，需分开维护设计和验证方案 |
| 决策 | 基本无方案分歧 | 有少量设计选择 | 有重大选型、替代方案或长期影响 |
| 验证记录 | 任务中关联 AC 与必要结果 | SPEC 能容纳验证计划与结果 | 验证策略和执行结果需分开维护、跨方协作 |

只有单份 SPEC 难以清楚维护需求、设计和验证责任时才采用 L3；不要求把所有 L3 章节写满。高风险仍必须覆盖必要设计、验证和独立 Review，但不单凭风险档位生成整套文档。

## 3. 最低产物

### L1 小改动

- 一份 `TASK`，可以直接放在 Issue、任务卡或 PR/MR 描述中。
- 完成验收和交付检查；Align 只确认 TASK、实现和验证一致。
- 不要求单独 PRD、SDD 或测试方案。

### L2 标准功能

- 一份 `SPEC.md`，集中记录需求、设计、测试和交付信息。
- 关键决定可直接写在 SPEC；只有长期或重大决定才另建 ADR。
- Align 逐项检查 SPEC 的 AC、关键设计和验证结果，结论放在交付清单。
- 不要求把 SPEC 拆成多份文档。

### L3 需分开维护的复杂项目

- 从 [正式功能模板包](templates/FORMAL-FEATURE/README.md) 开始，使用完整正规流程。
- `PRD.md`：需求意图、质量要求、假设与验收基线。
- `CLIENT-SDD.md`、`SERVER-SDD.md` 或单份 `SDD.md`：按受影响端记录关键设计、责任边界、失败处理和兼容回滚。
- `TEST-PLAN.md`：阻断级映射、跨层验证、专项测试和测试准入/退出条件。
- `DELIVERY.md`：交付基线、最终实现、测试结果、偏移、Align 和交付决定的唯一汇总。
- `ADR`：仅用于重大且难逆的决策。
- Align 对齐 PRD、SDD、ADR、可执行契约和测试证据；默认记录在 DELIVERY，仅独立审计场景另建 ALIGNMENT-GATE。
- PRD 和 DELIVERY 保持单一；Client/Server 设计都具有独立复杂度时拆分 SDD，只影响一端时只创建该端文件。

## 4. 验证与 Review 强度

验证档位独立于文档等级选择；文档精简不降低验证或 Review 要求。

| 档位 | 通常适用 | 最低验证 | Review |
| --- | --- | --- | --- |
| FAST | 低风险且易回滚 | 定向构建/测试或可复现人工验证；影响启动或用户主路径时加冒烟 | 作者自检；遵循团队原有 PR 要求 |
| STANDARD | 影响范围明确、失败后果可隔离的常规变化 | 单元 + 必要集成；Client/Server 受影响路径冒烟；跨端变化做契约/联调验证 | 作者自检 + 独立同事或只读 AI Review |
| CRITICAL | 失败后果重大或风险难以隔离的变化，不限文档等级 | STANDARD + 命中风险的白盒、E2E、故障注入、迁移/回滚、安全、性能验证 | 独立 Review；高风险结论由 Owner 接受 |

所有档位都必须验证本次改变的行为。CRITICAL 不等于执行所有测试类型，只执行能证明已识别风险的检查。

涉及权限绕过、资金或隐私泄露、数据损坏、破坏性契约或迁移、难回滚，或并发/性能问题可能造成大范围故障时，采用 CRITICAL。涉及公共契约、持久化、状态机或热点本身只是影响扫描线索，应先确认兼容性、影响范围与失败后果。

例如：局部权限修复可采用 L2 + CRITICAL，在 SPEC 中覆盖风险与独立 Review；向后兼容的可选协议字段可采用 L2 + STANDARD，但必须检查默认值、旧端兼容和契约；跨团队的大型低风险功能可采用 L3 + STANDARD。已有团队安全和发布要求仍适用。

## 5. 快速判断

```text
任务说明是否足以说清目标、改动、验收和必要风险？
├─ 是 → L1
└─ 否
   └─ 单份 SPEC 是否能清楚维护需求、设计和验证责任？
      ├─ 是 → L2
      └─ 否 → L3

另按实际影响、兼容性和失败后果选择 FAST / STANDARD / CRITICAL。
```

## 6. 升降级

- 实现中出现新风险先重新选择验证档位；必要信息无法在现有文档清楚维护时再升级文档等级。
- Intake 影响扫描发现公共契约、持久化、跨系统、安全、迁移或难回滚风险时，必须重新判断验证档位与文档需求，不能沿用未经复核的轻量结论。
- 需求收缩后可以降级，但已经产生且仍有维护价值的设计决定应保留。
- 不允许为了规避评审故意拆成多个 L1。
- 不允许仅因模板已经创建就维持不必要的高等级。
