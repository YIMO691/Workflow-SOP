# 代码、注释与复杂度规范

## 1. 目标

代码应优先通过命名、类型、边界和小函数表达意图；注释补充代码无法表达的原因、约束和风险。注释与代码一起评审，错误或过期注释视为缺陷。

## 2. 应该写的注释

- 公共 API、公共类型：用途、前置条件、可观察副作用和失败语义。
- 复杂业务规则：为什么这样处理、不变量、特殊边界和规则来源。
- 生命周期、并发、缓存、安全和性能敏感逻辑：成立条件与不可随意改变的约束。
- 临时兼容或 TODO：关联任务、Owner 和删除条件。

不要注释显而易见的控制流，不在源码中维护文件变更日志，不用长注释掩盖可以通过重命名或拆分解决的结构问题。

## 3. 时间与空间复杂度

不强制每个方法都写 Big-O。以下情况应在方法文档注释中记录：

- 非平凡算法、集合变换、递归或嵌套遍历。
- 高频调用、主线程路径、内存热点或已有性能目标的方法。
- 复杂度不直观，后续维护可能无意中退化的实现。

示例：

```text
复杂度：Time O(n log n)，Space O(n)，n 为候选项数量。
约束：n 当前上限为 10,000；超过上限必须分批。
```

数据库、网络和异步业务方法只写 Big-O 往往会误导，优先记录外部调用次数、批量上限、最坏扇出、分配量、锁/阻塞和主线程影响：

```text
性能：1 次批量查询，无逐项 RPC；内存 O(pageSize)。
并发：同一业务键串行，不持锁执行外部 I/O。
```

无法证明的复杂度不得由 AI 猜测。性能优化必须先定位热点；重要优化保留基线、方法和前后结果。

## 4. 通用代码要求

- 遵循目标仓库现有语言、命名、格式化、错误处理和注释风格。
- 一次变更聚焦一个可验证行为，避免顺手重写无关代码。
- 公共契约、持久化、兼容和失败语义变化必须同步相应 SDD/ADR 与测试。
- 不通过删除测试、放宽断言、吞掉异常或默认成功来获得绿色结果。
- 生成代码和机械接口定义保持单一来源，不在手写代码或文档中复制维护。
- 提交前清除调试入口、临时开关、无效日志、死代码和已失真的注释。

## 5. Review 检查

Review 优先检查正确性、边界、失败路径、并发/生命周期、兼容和验证证据。风格问题尽量交给格式化与静态检查；性能结论必须有规模假设或测量依据。
