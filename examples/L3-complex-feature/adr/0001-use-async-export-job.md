# 示例决策：采用持久化后台导出任务

状态：教学提议，未作为真实项目决策批准。依据 [需求](../PRD.md) 和 [设计](../SDD.md)。

## 选择与理由

将耗时导出从请求响应中分离，使用项目已有的持久化任务机制，让用户可查询状态，并为执行中断提供恢复依据。原请求内同步生成的方案较简单，但难以满足示例的大数据量交互目标。

## 代价与重新评估

需要维护任务状态、幂等和文件清理，恢复语义也更复杂。若真实数据量很小、同步处理能满足约定目标，应重新评估是否需要后台任务。这里没有测量数据，不声称该方案已经带来性能或成本收益。
