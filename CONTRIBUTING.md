# 贡献指南

## 开始之前

本指南用于维护 Workflow-SOP 自身；在业务项目中使用流程请从 [START-HERE](START-HERE.md) 开始。`main` 是当前协作基线，版本与试运行状态见 [README](README.md) 和 [CHANGELOG](CHANGELOG.md)。

- 使用问题选择“使用问题”Issue 表单。
- 明确错误选择“规范或模板缺陷”。
- 流程优化选择“流程优化建议”，并提供真实场景和影响。
- 不在 Issue、PR、提交或示例中放入秘密、生产数据、个人信息或未脱敏公司材料。

## 变更原则

1. 优先减少重复和无效成本。
2. 新增强制项必须对应已发生风险、法规安全要求或重复出现的交接失败。
3. 同一规则只保留一个权威来源，其他文件使用链接。
4. L1/L2 不因模板增加而自动变重。
5. 没有真实实现或验证证据时不得将 Align 标为 Pass。

## 开发流程

1. 从 `main` 创建短生命周期分支，建议使用 `docs/`、`fix/`、`feat/` 或 `workflow/` 前缀。
2. 更新 SOP 或模板时，同时检查 README、示例、AI 操作层和 GitHub 模板是否受影响。
3. 本地运行：

   ```powershell
   pwsh ./scripts/validate-workflow.ps1
   ```

   修改 Gate、状态或一致性规则时，必须在 `tests/workflow-gate/` 同时增加能够通过和应被拒绝的回归夹具。

4. 使用 PR 模板说明问题、证据、影响和 Align 结论。
5. 至少由 CODEOWNERS 中的维护者评审后合并。

GitHub 检查在 PR、main 推送和手动触发时运行。仓库目前采用 Squash merge，合并后自动删除远端任务分支；存在依赖的 PR 先合并前置改动，再核对后续差异和检查结果。遵循仓库实际设置，不把文档约定当作已启用的分支保护。

普通文字和导航修订运行现有检查即可；只有改变 Gate、状态或一致性规则时才增加对应正反例，不为措辞修改堆积测试。检查输出证明其覆盖范围内的结果，业务语义和实际验证证据仍需评审。

## 提交建议

- 使用聚焦且可回滚的提交。
- 提交标题使用祈使句并说明结果，例如 `Add scoped authority checks to Align Gate`。
- 纯移动或重命名尽量与内容修改分开，便于评审。

## 流程改进结论

维护者应将建议标记为接受、试验、拒绝或暂缓，并说明理由。试验中的变化应有范围、观察指标和复审时间。
