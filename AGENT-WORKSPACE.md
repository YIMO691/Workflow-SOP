# AgentWorkspace 工作区规范

## 1. 目的

AgentWorkspace 用于隔离 AI/自动化产生的任务记录、临时文件和验证证据，防止污染源码仓库，也让中断、换人和恢复任务有稳定入口。它不是另一份源码仓库，也不替代 Issue、PR/MR 和项目内权威文档。

## 2. 精简目录

```text
<AgentWorkspaceRoot>/<Project>/
├── 00_CONTROL/               # 团队规则、模板、入口脚本
├── 10_WORK/
│   └── active/<TASK-ID>/     # 可持续任务记录、检查点、交接材料
├── 20_SHARED/                # 经确认可复用的知识和资产
├── 30_EVIDENCE/<TASK-ID>/    # 测试日志、截图、报告和验证证据
├── 40_CORPUS/                # 只读参考仓库、资料或快照
├── _scratch/<TASK-ID>/       # 可随时删除的中间产物
└── _archive/                 # 已关闭但需保留的任务资料
```

`50_OPERATIONS` 仅在团队确有部署/运维自动化时增加；`90_LEGACY` 仅用于迁移期，不作为新团队默认目录。

## 3. 存储边界

| 内容 | 存放位置 |
| --- | --- |
| 源码、与维护者长期相关的 PRD/SDD/测试说明 | 项目仓库 |
| 任务检查点、恢复信息和交接材料 | `10_WORK/active/<TASK-ID>` |
| 可复现测试日志、截图、扫描报告 | `30_EVIDENCE/<TASK-ID>` |
| 下载、缓存、生成中间文件 | `_scratch/<TASK-ID>` |
| 团队确认可复用且不含秘密的资料 | `20_SHARED` |

DELIVERY 只链接证据位置和摘要，不复制大段日志。凭据、令牌、个人数据和生产数据不得进入 Workspace 文档或版本库。

## 4. 任务生命周期

1. 开始：创建任务目录，记录目标、源码仓库、分支、当前 Gate 和权限边界。
2. 执行：关键增量后更新六项检查点——已完成、当前基线、验证、偏移、阻断、下一步。
3. Stop：保留可恢复检查点，不宣称完成。
4. Resume：先核对源码版本、指令和环境是否漂移，再从下一步继续。
5. Done：项目内权威文档完成 Align；证据可定位；临时目录按团队保留策略清理；必要材料转入归档。
6. Cancel：记录取消原因和剩余影响，不把取消任务标记为 Done。

自动重试同一失败最多两轮；继续失败、环境不可信或影响未知时转为 Blocked，由人员决定下一步。

## 5. 最小任务索引

```text
任务：TASK-ID / 标题
源码：仓库、分支、Commit
当前 Gate：Ready / Build / Loop / Align / Done
权限与禁止操作：
已完成：
验证与证据：
偏移或阻断：
下一步：
```

团队可把这段放入现有任务系统；已有等价能力时，不要求再创建重复文件。
